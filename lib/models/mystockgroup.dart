import 'package:charts_flutter/flutter.dart';

// Class used for chart data
class MyStockGroup {
  String name;
  num quantity;
  Color colorOfBar;

  MyStockGroup({this.name, this.quantity, this.colorOfBar});
}
